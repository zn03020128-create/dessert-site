# 甜點日和｜Design System

## 1. 設計目標

網站以「溫柔、精緻、帶有甜點店櫥窗感」為核心。整體視覺應避免過度花俏，重點放在留白、圓角、柔和暖色與清楚的內容層級，使網站同時適合作品集、甜點推薦、品牌內容頁與課程展示。

## 2. 視覺關鍵字

- 溫暖、柔和、精緻
- 奶油色、莓果粉、可可棕
- 大留白、大標題
- 圓角卡片
- 細緻陰影
- 編輯誌風格

## 3. 色彩系統

| Token | 色碼 | 用途 |
|---|---|---|
| `--bg` | `#FFFAF7` | 全站背景 |
| `--surface` | `#FFFFFF` | 卡片、彈窗 |
| `--surface-soft` | `#F8EEE9` | 柔和分區背景 |
| `--text` | `#3F302D` | 主要文字、按鈕 |
| `--muted` | `#806B66` | 次要說明文字 |
| `--accent` | `#D98585` | 主題強調色 |
| `--accent-dark` | `#B96464` | Hover、重要標記 |
| `--accent-soft` | `#F6DADA` | 淡粉背景 |
| `--line` | `#EADBD5` | 邊框與分隔線 |

## 4. 字體

- 中文／介面文字：`Noto Sans TC`
- 英文大標題／裝飾文字：`Playfair Display`
- 備援：`sans-serif` / `serif`

### 字級建議

- H1：`46–82px`，使用 `clamp()` 響應式縮放
- H2：`30–48px`
- H3：`24px`
- 內文：`16–18px`
- 標籤／Eyebrow：`12–14px`

## 5. 間距

以 8px 為基礎單位：

- 小間距：8 / 12 / 16px
- 中間距：20 / 24 / 32px
- 大間距：48 / 64 / 84px

Section 垂直距離：桌面版約 84px；手機版約 64px。

## 6. 圓角

- 大型容器：30–38px
- 卡片：20–30px
- 小型元件：12–16px
- Chip / Button：999px 膠囊圓角

## 7. 陰影

主要陰影：

```css
box-shadow: 0 20px 60px rgba(88, 54, 46, .12);
```

只用於 Hover 卡片、Hero 主視覺與彈窗，避免全站過度立體。

## 8. 響應式原則

### Desktop

- 最大內容寬度：1160px
- Hero：雙欄
- 甜點卡片：3 欄

### Tablet

- Hero：改單欄
- 卡片：2 欄

### Mobile

- 主導覽改漢堡選單
- 卡片：1 欄
- 主要內容左右留白 14px
- 聯絡 CTA 改垂直排列

主要 breakpoint：

```css
@media (max-width: 860px) { ... }
@media (max-width: 620px) { ... }
```

## 9. 內容元件規格

每個甜點條目由 JavaScript 資料物件產生，建議維持以下結構：

```js
{
  id: 'unique-id',
  title: '甜點名稱',
  category: '類別',
  tags: ['標籤1', '標籤2'],
  summary: '卡片摘要',
  image: '圖片網址',
  content: {
    text: '完整文字內容',
    video: '影片網址',
    html: '<div>HTML 作品</div>',
    slides: '可嵌入的簡報網址'
  }
}
```

## 10. 擴充規則

新增甜點時，只新增一筆資料，不修改 HTML 結構。若未提供某類內容，詳情彈窗會顯示預設提示。

未來可增加：

- 分類篩選器
- 關鍵字搜尋
- 收藏功能
- 多頁面作品詳情
- JSON / CMS 資料來源
- Lightbox 圖片瀏覽
- YouTube / Vimeo 嵌入元件
- Google Slides / Canva 簡報嵌入

## 11. 互動規範

- Hover 位移不超過 5px
- 動畫時間約 0.25 秒
- Modal 支援 ESC 關閉
- 導覽列手機版需可收合
- 所有圖片需有 `alt`
- 互動按鈕需保留鍵盤可操作性

## 12. 圖片風格

建議使用：

- 自然光甜點攝影
- 暖色背景
- 乾淨桌面
- 淺景深
- 食物主體清楚

圖片比例建議統一使用 `4:3`，並以 `object-fit: cover` 呈現。
